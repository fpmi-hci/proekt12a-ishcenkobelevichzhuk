app{
  server {
    port = ${PORT}
    host = ${HOST}
    productTopicName = "products"
    producerConfigPath = "producer.conf"
  }
  db {
    provider = "postgres"
    driver = "org.postgresql.Driver"
    url = ${JDBC_DATABASE_URL}
    user = "postgres"
    password = "admin"
    migrationLocation = "migration"
  }
  email {
    url = "smtp://smtp.gmail.com:465"
    email = "supplier.server@gmail.com"
    user = "supplier.server"
    password = "cxgdwwwgpoivghnh"
    delay = 1
    productTopicName = "products"
    pollingTimeout = 5
    consumerConfigPath = "consumer.conf"
  }
}